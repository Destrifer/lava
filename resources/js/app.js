import { createApp } from 'vue'; // Импортируйте createApp из 'vue'
import './bootstrap';
import App from './App.vue';

createApp(App).mount("#app"); // Убедитесь, что селектор правильный, используйте "#app" для указания id
